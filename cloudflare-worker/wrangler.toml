name = "blog-stats"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量 (部署时在 Cloudflare Dashboard 设置，或使用 wrangler secret)
# [vars]
# ALLOWED_ORIGIN = "https://your-blog-domain.com"

# 敏感信息使用 secret (命令行设置):
# wrangler secret put GITHUB_TOKEN
# wrangler secret put GIST_ID
